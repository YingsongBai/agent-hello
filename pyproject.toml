[project]
name = "agent-hello"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "baiyingsong", email = "chuhe@micous.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
agent-hello = "agent_hello:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
